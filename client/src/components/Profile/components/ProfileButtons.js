import React from 'react';
import { Button } from 'reactstrap';

const ProfileButtons = props => {
  return (
    <section className="m-element-spacing">
      <Button className="s-btn-responsive">Edit Profile</Button>
    </section>
  );
};

export default ProfileButtons;
